<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>

  <title>Document</title>
</head>
<body>
  <h1 class="text-center text-4xl font-medium my-4"><%= camp.title %> </h1>
  <p class="text-2xl text-center mb-2"><%= camp.location %></p>

  <footer class="flex w-fit mx-auto gap-4">
    <a href='/campgrounds/<%= camp._id %>/edit' class="p-2 px-4 rounded-md bg-orange-500 block  text-center">Edit</a>
  <form action="/campgrounds/<%= camp._id %>?_method=Delete" method="post">
    
    <button type="submit" class=" p-2 px-4 rounded-md border border-red-500">Delete</button>
  </form>    
  <a href='/campgrounds' class=" p-2 px-4 rounded-md border border-orange-500">All campgrounds</a>
  </footer>
</body>
</html>