<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>

  <title>Edit Campground</title>
</head>
<body>
  
  <form action="/campgrounds/<%= camp._id %>?_method=PUT" method="post" class="flex flex-col gap-4 max-w-2xl mx-auto mt-8">
    <h1 class="text-4xl font-medium text-center">Edit Camp</h1>
    <div class="flex flex-col p-4 gap-2">
      <label   for="title">Title</label>
      <input  class="p-2 border border-gray-300" type="text" id="title" name="campground[title]" value="<%= camp.title %>">
    </div>
    <div class="flex flex-col p-4 gap-2">
      <label  for="location">Location</label>
      <input class="border border-gray-300 p-2" type="text" id="location" name="campground[location]" value="<%= camp.location %>">
    </div>
    <button type="submit" class="bg-orange-400 rounded-md text-white p-2 px-4 w-fit mx-auto">Update Camp</button>
  </form>

  

  <a href="/campgrounds/<%= camp._id %>"   class="block  w-fit text-blue-600 p-2 px-4 m-2 rounded-md border border-blue-600 my-6">Back to campground</a>

</body>
</html>